name: Deploy to Production

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:

    runs-on: self-hosted

    steps:
    - run: |
        cd /home/actions/hacktheburgh-x
        docker compose -f compose-prod.yaml down
        git fetch
        git pull
        docker compose -f compose-prod.yaml up -d
